<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.application.status.mapper.ApplicationStatusMapper">  
  <select id="getapplicationStatuList">
  	 SELECT
  	*
  	FROM
  		APPLICATION_STATUS
        WHERE
    application_status_type &lt;&gt;  '지원취소'
  </select>
  
</mapper>